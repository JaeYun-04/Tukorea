🛠️ 주요 기능

문자열 파싱: 문자열 형태의 수식을 한 글자씩 읽어 피연산자와 연산자를 구분합니다.
스택 연산:
숫자(피연산자)를 만나면 스택에 push 합니다.
연산자를 만나면 스택에서 두 개의 값을 pop 하여 계산하고 결과를 다시 push 합니다.

📂 파일 구조

main.c: 후위 표기법 계산 로직 및 테스트 케이스 포함.
ArrayStack.h: 배열 기반의 스택 구조체 및 push, pop, init_stack 함수 정의.
